- hosts: localhost

  vars_prompt:
    - name: "configuration"
      prompt: "What system configuration do you want to install?"
  vars_files:
    - vars/{{configuration}}.yml

  roles:
    - role: common

    - role: home
      when: model == "home"

#    - role: vm
#      when: model == "vm"
